{
  "name": "mck-music-visuals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check api/media/[id].mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

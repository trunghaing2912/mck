{
  "name": "mck-music-visuals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check api/media/[id].mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
